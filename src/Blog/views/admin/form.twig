{{ field('name', item.name, "Titre de l'article") }}
{{ field('slug', item.slug, "Lien") }}
{{ field('content', item.content, "Content", {type: 'textarea'}) }}
{{ field('created_at', item.created_at, "Date de création", {class: 'datepicker'}) }}